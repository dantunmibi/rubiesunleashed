name: Send Archetype Reminders

# Schedule: Every Monday at 10:00 AM UTC
on:
  schedule:
    - cron: '0 10 * * 1'  # Every Monday at 10 AM UTC
  
  # Allow manual trigger from GitHub
  workflow_dispatch:

jobs:
  send-reminders:
    runs-on: ubuntu-latest
    
    steps:
      - name: Send Archetype Reminder Emails
        run: |
          response=$(curl -s -w "\n%{http_code}" -X GET \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json" \
            https://www.rubiesunleashed.app/api/cron/send-archetype-reminders)
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "HTTP Status: $http_code"
          echo "Response Body: $body"
          
          if [ "$http_code" -ne 200 ]; then
            echo "❌ Cron job failed with status $http_code"
            exit 1
          fi
          
          echo "✅ Cron job completed successfully"